import { fonts } from '@global/theme/fonts';
import { colors } from '@global/theme/colors';
import { spacing } from '@global/theme/spacing';
import { fontSizes } from '@global/theme/fontSizes';

export const theme = {
  colors,
  fonts,
  fontSizes,
  spacing,
} as const;
